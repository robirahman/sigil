[package]
name = "sigil_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "sigil_engine"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "abi3-py38"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
